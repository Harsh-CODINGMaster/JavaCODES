class Bag {

	float price;
	String color;

	Bag(float price, String color) {
		this.price = price;
		this.color = color;
	}

	int identifyDiscountAmount() {
		int discountAmount = 0;

		if (this.color.equals("Black")) {
			discountAmount = 10;
		}
		return discountAmount;
	}

}

class HandBag extends Bag {

	String shape;

HandBag (float price,String color, String shape)
{

super (price,color);

this.shape="Barrel";
}

int identifyDiscountAmount () {
	int discount Amount:
}

if(this.shape.equals("Bobo")) { discount Amount-13

else if(this. discount Amount:

shape.equals("")) (

return discountAmount:

int